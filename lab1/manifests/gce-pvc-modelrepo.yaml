apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gce-model-repo
  namespace: myapps
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard-rwo